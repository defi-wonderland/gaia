# Geo chain EVM-RPC provider.
#
# NOTE: This template is for reference only. The docker-compose entrypoint
# generates the actual provider file at runtime from GEO_RPC_URL.

kind = "evm-rpc"
network = "geo"
url = "${GEO_RPC_URL}"
